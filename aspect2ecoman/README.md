This directory contains the preprocessing script that converts the aspect PVD solution to the proper ECOMAN D-REX_M input format, and generates the associated input dat files for D-REX_M, VIZTOMO, and SKS-SPLIT.

```sbatch run_aspect2ecoman.slurm 28.35```